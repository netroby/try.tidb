Try TiDB
=========

Try TiDB on the browser, you can type any valid MySQL command, and it will return exec result as well as MySQL Does.
TiDB was the very success Database which can 100% compact with the MySQL Server.